---
phase: 02-spotlight-search
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/renderer/src/components/Search/SpotlightSearchBar.tsx
  - src/renderer/src/components/Search/SearchResultsList.tsx
  - src/renderer/src/App.tsx
  - src/renderer/src/components/DocumentFulfillmentCard/DocumentFulfillmentCard.tsx
  - src/renderer/src/components/DocumentFulfillmentCard/index.ts
  - electron-builder.json
autonomous: true
requirements:
  - SEARCH-06
  - SEARCH-07
  - BUILD-01
  - BUILD-02
  - BUILD-03
must_haves:
  truths:
    - "Clicking a search result animates search bar center→top-left"
    - "Content area cross-fades to show DocumentFulfillmentCard"
    - "Animation is ~250-400ms spring, not jump-cut"
    - "Project builds to NSIS installer"
  artifacts:
    - path: "src/renderer/src/App.tsx"
      provides: "Animated search-to-fulfillment flow"
---

<objective>
Implement the center-to-corner animation for the search bar, cross-fade content transition to DocumentFulfillmentCard, and verify the build pipeline produces a working NSIS installer.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/02-spotlight-search/02-RESEARCH.md
D:/Dizaynn/zunoora-design/ZUNOORA-DESIGN-SPEC.md sections 8.3-8.4
D:/Dizaynn/promptt.md sections 4.3-4.4
</context>

<tasks>

<task type="auto">
  <name>Create DocumentFulfillmentCard placeholder</name>
  <files>
    src/renderer/src/components/DocumentFulfillmentCard/DocumentFulfillmentCard.tsx
    src/renderer/src/components/DocumentFulfillmentCard/index.ts
  </files>
  <action>
    Create components/DocumentFulfillmentCard/ directory:

    DocumentFulfillmentCard.tsx:
    - Centered A4-proportioned card (max-w-[595px] = A4 width at 72dpi)
    - Styled like paper: bg-[var(--paper-bg)], text-[var(--paper-text)]
    - Rounded corners, subtle shadow
    - paper-noise class applied for texture
    - Contains placeholder content: "Shablon Fulfillment" title + "This is where the document form will appear" text
    - Framer Motion motion.div with spring animation for entry (stiffness: 160, damping: 24, delay: 0.15)
    - Prop: isVisible to control mount/unmount with AnimatePresence

    index.ts: export DocumentFulfillmentCard
  </action>
  <acceptance_criteria>
    - DocumentFulfillmentCard renders as A4-proportioned centered card
    - Has paper-noise class and paper styling
    - Uses framer-motion for enter animation
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Implement center-to-corner animation + content cross-fade</name>
  <files>
    src/renderer/src/components/Search/SpotlightSearchBar.tsx
    src/renderer/src/App.tsx
    src/renderer/src/components/Stage/MainStage.tsx
  </files>
  <action>
    Update SpotlightSearchBar.tsx:
    - Use useSearchStore isDocked state
    - When isDocked: bar shrinks and moves to top-left (fixed position in corner, width ~280px)
    - When !isDocked: bar stays centered
    - Use Framer Motion layout + AnimatePresence for smooth position/size transition
    - Docked state shows a compact version (mini bar) that's always visible

    Update MainStage.tsx:
    - Accept a docked/: boolean prop
    - When !docked: show centered brand content (Zunoora title + subtitle)
    - When docked: hide brand content (AnimatePresence fade out)

    Update App.tsx:
    - Import SpotlightSearchBar from Search
    - Import DocumentFulfillmentCard
    - Import useSearchStore
    - State: track selectedResult
    - When a result is selected:
      1. setDocked(true) on search store
      2. set selectedResult
    - Layout:
      - Left: Sidebar
      - Right: container with:
        - SpotlightSearchBar (positioned absolutely, centered when !docked, top-left when docked)
        - MainStage (behind the search bar, shows/hides brand)
        - DocumentFulfillmentCard (appears when selectedResult is set, with cross-fade)
    - AnimatePresence for content transitions (mode="wait" or cross-fade)
  </action>
  <acceptance_criteria>
    - Clicking result triggers smooth bar animation center→top-left (~250-400ms)
    - DocumentFulfillmentCard appears with fade/slide transition
    - Brand text fades out when docked
    - Animation is spring-based, not jump-cut
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Verify build pipeline (electron-builder NSIS)</name>
  <files>
    electron-builder.json
  </files>
  <action>
    Read electron-builder.json and verify:
    - win.target includes "nsis"
    - appId and productName are correct
    - icon path points to build/icon.png
    - npm run build succeeds
    - npm run dist (or electron-builder) can build the project

    Fix any issues found in electron-builder.json config.
  </action>
  <acceptance_criteria>
    - npm run build succeeds
    - electron-builder.json has valid NSIS config
    - Builder config ready for dist
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds
- [ ] Clicking a result animates bar to top-left
- [ ] Content cross-fades to DocumentFulfillmentCard
- [ ] Animation is smooth (~250-400ms), not instant
- [ ] electron-builder config is correct
</verification>

<success_criteria>
- Search bar animates to top-left on selection
- Content cross-fades to DocumentFulfillmentCard
- All animation parameters match design spec
- Build pipeline is verified
</success_criteria>

<output>
After completion, create `.planning/phases/02-spotlight-search/02-02-SUMMARY.md`
</output>
