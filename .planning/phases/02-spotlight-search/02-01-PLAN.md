---
phase: 02-spotlight-search
plan: 01
type: execute
wave: 1
depends_on: ["01-04"]
files_modified:
  - src/renderer/src/components/Search/SpotlightSearchBar.tsx
  - src/renderer/src/components/Search/SearchResultsList.tsx
  - src/renderer/src/components/Search/NewShablonAction.tsx
  - src/renderer/src/components/Search/index.ts
  - src/renderer/src/store/useSearchStore.ts
  - src/renderer/src/locales/uz.json
  - src/renderer/src/locales/ru.json
  - src/renderer/src/locales/en.json
autonomous: true
requirements:
  - SEARCH-01
  - SEARCH-02
  - SEARCH-03
  - SEARCH-04
  - SEARCH-05
must_haves:
  truths:
    - "Search bar is centered in idle state"
    - "Search bar and results look like one cohesive element"
    - "Typing shows debounced live results"
    - "+ New shablon is pinned above results"
    - "Empty query + focused shows recent suggestions"
  artifacts:
    - path: "src/renderer/src/components/Search/SpotlightSearchBar.tsx"
      provides: "Spotlight-style search bar with all states"
    - path: "src/renderer/src/components/Search/SearchResultsList.tsx"
      provides: "Results list with NewShablonAction pinned"
  key_links:
    - from: "App.tsx"
      to: "SpotlightSearchBar"
      via: "React component import"
---

<objective>
Build the Spotlight search UI: SpotightSearchBar (idle/focused states), SearchResultsList with NewShablonAction pinned, debounced live results, and the unified visual look where bar + results share a seamless border/shadow.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/02-spotlight-search/02-RESEARCH.md
D:/Dizaynn/zunoora-design/ZUNOORA-DESIGN-SPEC.md sections 8.3-8.4
D:/Dizaynn/promptt.md sections 4.1-4.3
</context>

<tasks>

<task type="auto">
  <name>Update useSearchStore with debounce-ready fields</name>
  <files>
    src/renderer/src/store/useSearchStore.ts
  </files>
  <action>
    Update the existing search store to include:
    - Add `recentSuggestions: string[]` field (static placeholder data for empty-query state)
    - Add `debouncedQuery: string`
    - Keep existing fields: query, isFocused, isDocked, results
    - Keep `setQuery` that also updates debouncedQuery (we'll handle real debounce in component)
  </action>
  <acceptance_criteria>
    - useSearchStore exports all required fields
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Create NewShablonAction component</name>
  <files>
    src/renderer/src/components/Search/NewShablonAction.tsx
    src/renderer/src/components/Search/index.ts
  </files>
  <action>
    Create components/Search/ directory with:

    NewShablonAction.tsx:
    - Simple row component: Plus icon (lucide Plus, h-4 w-4, strokeWidth 1.5) + label
    - Label uses t() for "New shablon" (use existing key "search.newShablon" in locales)
    - Styling: full-width row with px-3 py-2.5, hover:bg-[var(--surface-hover)], cursor-pointer
    - Divider/border-b at bottom to separate from results
    - OnClick calls a provided onSelect callback prop
    - Uses motion.div from framer-motion with initial/animate for list entry

    index.ts: export NewShablonAction
  </action>
  <acceptance_criteria>
    - NewShablonAction exists with Plus icon and t() label
    - Has onSelect prop
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Create SearchResultsList component</name>
  <files>
    src/renderer/src/components/Search/SearchResultsList.tsx
  </files>
  <action>
    Create SearchResultsList.tsx:
    - Receives results: string[], isFocused: boolean, onSelect: (result: string) => void
    - States:
      - Empty query + focused: show recentSuggestions from store
      - Empty + not focused: hidden (null)
      - Typing (query non-empty): show results
      - No results for query: show "No results found" from t()
    - Renders NewShablonAction as pinned first row above results
    - Result items: text-muted-foreground, hover:bg-[var(--surface-hover)], px-3 py-2.5, text-xs
    - Each result has onClick → onSelect(result)
    - Uses AnimatePresence + motion.div for smooth list enter/exit
    - The outer container has rounded-b-2xl to match search bar bottom, hairline border (top border from search bar), surface background
    - This container does NOT have its own top border — it visually joins with search bar
  </action>
  <acceptance_criteria>
    - SearchResultsList renders NewShablonAction pinned above results
    - Has empty, loading, populated states
    - Shows recent suggestions when empty + focused
    - Uses AnimatePresence
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Create SpotlightSearchBar component</name>
  <files>
    src/renderer/src/components/Search/SpotlightSearchBar.tsx
  </files>
  <action>
    Create SpotlightSearchBar.tsx:
    - Props: onSelect: (result: string) => void
    - Uses useSearchStore for query, setQuery, isFocused, setFocused, results, recentSuggestions
    - Uses useTranslation for placeholder text
    - The entire component (bar + results) is wrapped in a container div (max-w-xl mx-auto w-full)
    - The search bar itself:
      - Rounded-2xl (16px) when results NOT visible
      - Rounded-t-2xl + rounded-b-none when results visible (bar's bottom corners change so results join seamlessly)
      - Height: 48px (h-12) in idle, expands when focused
      - Inner: flex row with optional icon prefix + input + optional suffix
      - Input: flex-1, bg-transparent, outline-none, placeholder text-muted-foreground, text-sm
      - Focus states: border-hairline, bg-[var(--surface)], backdrop blur
    - Below the bar (inside same outer container): SearchResultsList
      - Only rendered when isFocused or query.length > 0
    - Uses Framer Motion layout for smooth height transition on focus
  </action>
  <acceptance_criteria>
    - SpotlightSearchBar renders input with placeholder from t()
    - Search bar + results look like one unified element (shared border/shadow)
    - Typing updates store query with debounce
    - Focus/blur handled properly
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Update App.tsx to integrate spotlight search</name>
  <files>
    src/renderer/src/App.tsx
  </files>
  <action>
    Update App.tsx:
    - Import SpotlightSearchBar and MainStage
    - Replace the current static centered content in MainStage with:
      - SpotlightSearchBar rendered absolutely centered when not docked
      - When docked, SpotlightSearchBar goes to top-left
    - Keep sidebar as is
    - Remove the hardcoded Zunoora title from MainStage (move to search bar area)
    - Wait — actually keep MainStage for now. The search bar sits ON TOP of the stage content.
    - Layout:
      - MainStage with desk-vignette contains centered Zunoora brand (move this inside a conditional — hide when docked)
      - SpotlightSearchBar rendered above MainStage, centered when not docked
    - Actually simpler approach: SpotlightSearchBar is inside MainStage, and controls whether brand text is shown
  </action>
  <acceptance_criteria>
    - App renders sidebar + main area with search bar centered
    - Build succeeds
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds
- [ ] Search bar is centered on load
- [ ] Clicking search bar shows results area (unified element)
- [ ] Typing shows debounced results after ~150-200ms
- [ ] "+ New shablon" appears above results
- [ ] Empty query + focused shows recent suggestions
</verification>

<success_criteria>
- App launches with search bar centered
- Search bar + results are one unified visual element
- Typing produces debounced live results
- "+ New shablon" always visible when results shown
- Empty query + focused state shows suggestions (no dead end)
</success_criteria>

<output>
After completion, create `.planning/phases/02-spotlight-search/02-01-SUMMARY.md`
</output>
