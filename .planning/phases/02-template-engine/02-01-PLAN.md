---
wave: 1
id: "02-01"
depends_on: []
files_modified:
  - "new: src/renderer.ts"
  - "new: src/menu.ts"
  - "new: src/templates.ts"
  - "modify: src/db.ts" (add teacher_visible to Shablon type)
  - "modify: src/index.ts" (wire menu into main flow)
autonomous: true
---

# Plan 02-01: Template Engine, Menu & Role Filter

## Objective
Implement the template engine, template loader with Supabase+local fallback, role-filtered menu, and update the entrypoint.

## Tasks

### Task 1: Add teacher_visible column to shablons
<read_first>
- Supabase `shablons` table schema (from earlier exploration)
</read_first>

<action>
Create `supabase/migrations/002_add_teacher_visible.sql`:
```sql
ALTER TABLE shablons ADD COLUMN IF NOT EXISTS teacher_visible boolean DEFAULT false;
-- Mark teacher-visible templates
UPDATE shablons SET teacher_visible = true WHERE type IN (
  'ariza', 'bsb_chb', 'ish_tabeli', 'ktp', 'oum', 'sillabus',
  'hisobot_choraklik', 'tushuntirish_xati', 'dars_jadvali',
  'ota_ona_majlis_bayoni', 'tavsifnoma', 'dars_ozini_toldirish'
);
```

Update `Shablon` interface in `src/db.ts` — add `teacher_visible: boolean`.
</action>

<acceptance_criteria>
- Migration runs successfully
- Teacher-visible templates have `teacher_visible = true`
- TypeScript build passes with updated Shablon type
</acceptance_criteria>

### Task 2: Implement Blanks/shablons.json local cache + template loader
<read_first>
- `src/db.ts` — Shablon type, supabase client
- Supabase `shablons` table data
</read_first>

<action>
Create `src/templates.ts`:
- Function `loadShablons(context: UserContext): Promise<Shablon[]>`:
  1. Try `supabase.from('shablons').select('*').order('label')` 
  2. On success: write data to `Blanks/shablons.json` (write-through cache), return shablons
  3. On Supabase failure: try to read from `Blanks/shablons.json`
  4. On both fail: exit with Uzbek error "Shablonlarni yuklab bo'lmadi. Internet aloqangizni tekshiring."
- Function `filterShablonsByRole(shablons: Shablon[], role: 'teacher' | 'director'): Shablon[]`:
  - Teacher: filter where `teacher_visible === true`
  - Director: return all
- Create `Blanks/` directory

Add `teacher_visible` to the `Shablon` interface in `db.ts`.
</action>

<acceptance_criteria>
- Templates load from Supabase and cache to Blanks/shablons.json
- Second run with Supabase offline loads from local cache
- Teacher sees only teacher_visible templates
- Director sees all 23 templates
- TypeScript compiles
</acceptance_criteria>

### Task 3: Port renderer.js to TypeScript
<read_first>
- `D:\ZUNOORAA\shablon-filler\src\renderer.js` — existing engine
</read_first>

<action>
Create `src/renderer.ts`:
- Export function `renderTemplate(template: string, values: Record<string, string>): string`
- Keep exact logic from existing JS:
  - `template.replace(/{{(\w+)}}/g, ...)` with strict `\w+` pattern
  - Blank line collapsing: split by newline, trim empty lines, replace 3+ newlines with 2, trim edges
  - Warning for leftover `{{...}}` placeholders (with English warning message kept as-is, matching original)
- Import types from db.ts if needed (none needed for this function)
</action>

<acceptance_criteria>
- Template substitution works correctly: `{{name}}` → value
- Blank lines collapse properly (3+ → 2, leading/trailing removed)
- Leftover placeholders print warning to stderr
- TypeScript compiles
</acceptance_criteria>

### Task 4: Port menu.js to TypeScript
<read_first>
- `D:\ZUNOORAA\shablon-filler\src\menu.js` — existing menu
- `src/templates.ts` — loadShablons, filterShablonsByRole
- `src/db.ts` — UserContext, Shablon types
</read_first>

<action>
Create `src/menu.ts`:
- Export function `showMenu(context: UserContext): Promise<Shablon>`
- Replicate existing flow:
  1. Print welcome box with user name and school (same as Phase 1 but in menu)
  2. Call `loadShablons(context)` to get templates
  3. Apply role filter: `filterShablonsByRole(shablons, context.role)`
  4. Display numbered inquirer list
  5. Include "Chiqish" option
  6. Return selected shablon
- Keep Uzbek labels
- Handle empty list (no templates available) with Uzbek message and exit
</action>

<acceptance_criteria>
- Welcome box shows user name + school name
- Teacher sees only teacher-visible templates
- Director sees all templates
- "Chiqish" exits cleanly
- Empty state handled gracefully
- TypeScript compiles
</acceptance_criteria>

---

wave: 2
id: "02-02"
depends_on: ["02-01"]
files_modified:
  - "modify: src/index.ts" (wire renderer + menu, preview rendered template)
autonomous: true
---

# Plan 02-02: Wire Main Loop & Template Preview

## Objective
Update the main entrypoint to show the template menu, collect fields, render the template, and display a preview.

## Tasks

### Task 1: Update main entrypoint with template loop
<read_first>
- `src/index.ts` — current entrypoint (auth only)
- `src/menu.ts` — showMenu
- `src/renderer.ts` — renderTemplate
- `D:\ZUNOORAA\shablon-filler\index.js` — existing main loop (full flow)
- `D:\ZUNOORAA\shablon-filler\src\collector.js` — existing collector (for next phase ref)
</read_first>

<action>
Update `src/index.ts`:
- Import `showMenu` from `./menu.js`
- Import `renderTemplate` from `./renderer.js`
- Import `inquirer` for confirm prompts
- After auth, enter a loop:
  1. Call `showMenu(context)` to get selected shablon
  2. For Phase 2 (no collector yet): prompt for fields using a simple inquirer input for each `required` field (basic version — Phase 3 will add full auto-fill)
  3. Call `renderTemplate(shablon.template, values)`
  4. Display rendered preview with `Tayyor matn:` header and separator line
  5. Ask "Hujjat yaratilsinmi?" (confirm) — but for Phase 2, just confirm to exit (actual docx generation is Phase 4)
  6. Ask "Yana hujjat yaratish?" to loop
- Keep existing ExitPromptError handling
- Keep existing Ctrl+C clean exit
</action>

<acceptance_criteria>
- Full flow works: login → menu → basic field prompts → render preview → loop
- "Chiqish" from menu exits cleanly
- Template renders with placeholder substitution
- Preview shows properly formatted document text
- Ctrl+C exits at any point
- TypeScript compiles
</acceptance_criteria>

## Verification Criteria
1. All 23 templates load from Supabase
2. Teacher sees only 12 teacher-visible templates
3. Director sees all 23
4. Template renderer replaces all `{{placeholders}}` correctly
5. Locally cached shablons.json works when offline
6. Full end-to-end flow: login → select template → fill fields → preview
7. TypeScript build passes
