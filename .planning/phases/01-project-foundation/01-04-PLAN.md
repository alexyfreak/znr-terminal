---
phase: 01-project-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - src/renderer/src/App.tsx
  - src/renderer/src/components/Sidebar/Sidebar.tsx
  - src/renderer/src/components/Sidebar/index.ts
  - src/renderer/src/components/Stage/MainStage.tsx
  - src/renderer/src/components/Stage/index.ts
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
  - DZN-01
  - DZN-06
  - STATE-01
must_haves:
  truths:
    - "App launches with collapsed sidebar rail on the left"
    - "Main stage occupies the remaining space with centered content"
    - "Sidebar toggle button is visible in collapsed state"
    - "Sidebar shows the Zunoora logo/brand at top"
    - "Zunoora brand uses serif-italic styling per design spec"
  artifacts:
    - path: "src/renderer/src/components/Sidebar/Sidebar.tsx"
      provides: "Sidebar component with collapsed state"
      contains: "useSidebarStore"
    - path: "src/renderer/src/components/Stage/MainStage.tsx"
      provides: "Main stage with centered content"
      contains: "desk-vignette"
    - path: "src/renderer/src/App.tsx"
      provides: "Shell layout integrating sidebar and stage"
      contains: "Sidebar"
  key_links:
    - from: "App.tsx"
      to: "Sidebar.tsx"
      via: "React component import"
    - from: "App.tsx"
      to: "MainStage.tsx"
      via: "React component import"
    - from: "Sidebar.tsx"
      to: "useSidebarStore"
      via: "Zustand store hook"
      pattern: "useSidebarStore"
---
<objective>
Build the actual app shell UI with a collapsible sidebar (initially collapsed) and a centered main stage with the Zunoora brand, wired to Zustand stores and i18n.

Purpose: Deliver the visual starting state of the app — sidebar collapsed, Zunoora branding centered.
Output: Working app shell with sidebar rail, toggle, and centered stage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
D:/Dizaynn/zunoora-design/ZUNOORA-DESIGN-SPEC.md
D:/Dizaynn/zunoora-design/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Create Sidebar component with collapsed/expanded states</name>
  <files>
    src/renderer/src/components/Sidebar/Sidebar.tsx
    src/renderer/src/components/Sidebar/index.ts
  </files>
  <read_first>
    D:/Dizaynn/zunoora-design/ZUNOORA-DESIGN-SPEC.md section 8.2 (Sidebar)
    D:/Dizaynn/zunoora-design/SKILL.md (Sidebar item patterns, Layout Patterns)
  </read_first>
  <action>
    Create components/Sidebar/ directory with two files:

    Sidebar.tsx:
    - Import useSidebarStore, useTranslation from react-i18next
    - Framer Motion motion.aside with layout prop for smooth width animation
    - Width: isExpanded ? 260 : 60 (Tailwind: w-[260px] : w-[60px])
    - Transition: spring stiffness 180, damping 26 (from design spec)
    - Background: bg-[var(--sidebar)] (#080808)
    - Border right: border-r border-[var(--hairline)]
    - Layout: flex flex-col h-screen
    - Top section: logo/toggle area
      - In collapsed: diamond ◆ character (10×10px, warm color #F5F1E8) + toggle hamburger
      - In expanded: diamond ◆ + "ZUNOORA" label (14px/500/0.2em tracking) + toggle button
      - Brand mark spec: diamond rotated 45deg, warm border
    - Middle: placeholder history section
      - Collapsed: clock icon only
      - Expanded: "Tarix" header (serif-italic 10px/0.3em tracking) + empty state text
    - Bottom: Settings + Account
      - Settings: settings icon (Lucide Settings, h-3.5 w-3.5, strokeWidth 1.5)
      - Account: user icon (Lucide User, h-3.5 w-3.5, strokeWidth 1.5)
      - Both always visible, hover effects per design spec
    - Use t() from useTranslation for all labels
    - In collapsed state, only show icons; in expanded, show icons + labels
    - Hover: bg-[var(--surface-hover)] on items

    index.ts: export { default as Sidebar } from './Sidebar'
  </action>
  <acceptance_criteria>
    - Sidebar.tsx contains "useSidebarStore"
    - Sidebar.tsx contains motion.aside with layout prop
    - Sidebar.tsx contains "w-[260px]" and "w-[60px]"
    - Sidebar.tsx contains "stiffness: 180, damping: 26"
    - Sidebar.tsx contains "useTranslation"
    - Sidebar.tsx contains Settings and User lucide icons
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Create MainStage component with centered brand</name>
  <files>
    src/renderer/src/components/Stage/MainStage.tsx
    src/renderer/src/components/Stage/index.ts
  </files>
  <read_first>
    D:/Dizaynn/zunoora-design/ZUNOORA-DESIGN-SPEC.md section 8.1 (AppShell), section 8.3 (ChatPanel center mode idle state)
  </read_first>
  <action>
    Create components/Stage/ directory:

    MainStage.tsx:
    - Main container: flex-1 flex flex-col desk-vignette (import from globals.css)
    - Centered content area: relative flex min-h-0 flex-1 items-center justify-center
    - Inner content (idle state, matching design spec section 8.3):
      - "Zunoora" title in serif-italic, warm color, centered
      - Subtitle "What shall we draft today?" — 24px/500 weight
      - Description "A lesson plan, a quiz..." — 14px muted foreground
      - Empty prompt area below (will become SpotlightSearchBar in Phase 2)
      - Use t() from useTranslation for text strings

    index.ts: export { default as MainStage } from './MainStage'
  </action>
  <acceptance_criteria>
    - MainStage.tsx contains "desk-vignette"
    - MainStage.tsx contains "serif-italic" class
    - MainStage.tsx contains "useTranslation"
    - MainStage.tsx contains centered flex layout classes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Update App.tsx to integrate Sidebar + MainStage</name>
  <files>
    src/renderer/src/App.tsx
  </files>
  <read_first>
    src/renderer/src/App.tsx (current state from plan 01-02)
  </read_first>
  <action>
    Rewrite App.tsx:
    - Import Sidebar from './components/Sidebar'
    - Import MainStage from './components/Stage'
    - Layout: div.flex.h-screen.w-full.overflow-hidden
    - Left: Sidebar component
    - Right: MainStage component (flex-1)
    - The zustand stores and i18n are consumed internally by components
    - Clean, minimal — just the shell layout
  </action>
  <acceptance_criteria>
    - App.tsx imports Sidebar from './components/Sidebar'
    - App.tsx imports MainStage from './components/Stage'
    - Build succeeds with npm run build
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Add Framer Motion dependency and verify build</name>
  <files>
    package.json
  </files>
  <read_first>
    package.json
  </read_first>
  <action>
    Run: npm install framer-motion
    Then run "npm run build" to verify everything compiles.
  </action>
  <acceptance_criteria>
    - package.json contains "framer-motion" in dependencies
    - npm run build exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds
- [ ] npm run dev shows sidebar (collapsed) with logo + toggle + settings + account
- [ ] Sidebar toggle switches between collapsed (60px) and expanded (260px)
- [ ] MainStage shows centered Zunoora brand in serif-italic
- [ ] Animation is smooth (spring transition on sidebar width)
- [ ] Theme is dark carbon (#0A0A0A background)
</verification>

<success_criteria>
- All tasks completed
- App shell renders with collapsed sidebar and centered stage
- Sidebar toggle works with smooth spring animation
- All Zunoora design tokens applied
- Zustand stores provide state
- i18n provides translations
- Build succeeds with all dependencies installed
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-04-SUMMARY.md`
</output>
