---
phase: 01-project-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - package.json
  - electron.vite.config.ts
  - src/renderer/src/styles/globals.css
  - src/renderer/src/App.tsx
autonomous: true
requirements:
  - DZN-01
  - DZN-02
  - DZN-03
  - DZN-04
  - DZN-05
  - DZN-06
  - DZN-07
  - DZN-08
  - SHELL-02
must_haves:
  truths:
    - "Tailwind utility classes resolve to Zunoora design token values"
    - "Custom CSS utilities (serif-italic, paper-noise, desk-vignette) are available"
    - "Inter font is loaded from @fontsource/inter"
    - "Window background is carbon (#0A0A0A), text is foreground (#EDEDED)"
  artifacts:
    - path: "src/renderer/src/styles/globals.css"
      provides: "Tailwind v4 config with Zunoora design tokens"
      contains: "@theme inline"
    - path: "src/renderer/src/App.tsx"
      provides: "Root app component with dark theme applied"
      min_lines: 10
  key_links:
    - from: "src/renderer/src/styles/globals.css"
      to: "Tailwind utility classes"
      via: "@theme inline directive"
      pattern: "@theme inline"
---
<objective>
Implement the Zunoora design system using Tailwind CSS v4 with custom design tokens, font loading, and custom CSS utilities.

Purpose: Make the entire Zunoora visual identity available as Tailwind utility classes.
Output: globals.css with @theme inline tokens, custom utilities, Inter font, and App.tsx wired with dark theme.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
D:/Dizaynn/zunoora-design/ZUNOORA-DESIGN-SPEC.md
D:/Dizaynn/zunoora-design/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Add Tailwind, Inter font, and lucide-react dependencies</name>
  <files>
    package.json
    electron.vite.config.ts
  </files>
  <read_first>
    package.json
    electron.vite.config.ts
  </read_first>
  <action>
    Run: npm install tailwindcss @tailwindcss/vite -D && npm install @fontsource/inter lucide-react
    Then update electron.vite.config.ts to add the tailwindcss() plugin to the renderer.plugins array (after react()). Import: import tailwindcss from '@tailwindcss/vite'
  </action>
  <acceptance_criteria>
    - package.json contains "tailwindcss" in devDependencies
    - package.json contains "@fontsource/inter" in dependencies
    - package.json contains "lucide-react" in dependencies
    - electron.vite.config.ts contains "tailwindcss()" in renderer.plugins
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Create globals.css with Zunoora design tokens</name>
  <files>
    src/renderer/src/styles/globals.css
  </files>
  <read_first>
    D:/Dizaynn/zunoora-design/ZUNOORA-DESIGN-SPEC.md
    D:/Dizaynn/zunoora-design/SKILL.md
  </read_first>
  <action>
    Create src/renderer/src/styles/globals.css with exact content from RESEARCH.md Pattern 4:
    
    @import "tailwindcss";
    @import "@fontsource/inter/400.css";
    @import "@fontsource/inter/500.css";
    @import "@fontsource/inter/600.css";

    :root block with all CSS variables:
    --carbon: #0A0A0A; --surface: #111111; --surface-hover: #1A1A1A;
    --hairline: rgba(255,255,255,0.06); --warm: #F5F1E8;
    --foreground: #EDEDED; --muted-foreground: #8A8A8A;
    --sidebar: #080808; --popover: #0E0E0E;
    --input-border: rgba(255,255,255,0.08); --destructive: #C84A4A;
    --paper-bg: #FAFAF7; --paper-text: #1A1A1A;

    @theme inline block with all color, font, and radius tokens mapping to the CSS variables.

    Custom utilities: .serif-italic, .paper-noise, .desk-vignette (exact CSS from design spec).

    ::selection with oklch(0.94 0.015 85 / 0.25) background.

    Base body styles: bg-[var(--carbon)] text-[var(--foreground)] antialiased font-sans.
  </action>
  <acceptance_criteria>
    - globals.css contains "@import \"tailwindcss\""
    - globals.css contains "@theme inline"
    - globals.css contains "--carbon: #0A0A0A"
    - globals.css contains "--warm: #F5F1E8"
    - globals.css contains ".serif-italic" class
    - globals.css contains ".paper-noise" class
    - globals.css contains ".desk-vignette" class
    - globals.css contains "@fontsource/inter"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Wire App.tsx with dark theme shell</name>
  <files>
    src/renderer/src/App.tsx
  </files>
  <read_first>
    src/renderer/src/App.tsx (from scaffold)
  </read_first>
  <action>
    Rewrite App.tsx as a functional component that renders:
    - A full-screen flex container: className="flex h-screen w-full overflow-hidden bg-[var(--carbon)] text-foreground"
    - Left sidebar placeholder (260px when expanded, ~60px when collapsed — but for now render a minimal collapsed rail with a diamond logo character ◆ and the word ZUNOORA)
    - Main stage area (flex-1) with centered placeholder text "Zunoora" in serif-italic style
    - Use the Zunoora design: sidebar bg-[var(--sidebar)], main area with desk-vignette
    - Import from react (useState for isExpanded, default false)
    - No external dependencies yet — pure Tailwind CSS
  </action>
  <acceptance_criteria>
    - App.tsx contains "bg-[var(--carbon)]"
    - App.tsx contains sidebar with width 60px (collapsed state)
    - App.tsx contains flex layout h-screen w-full
    - Build succeeds with "npm run build"
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds
- [ ] npm run dev shows dark background with sidebar rail
- [ ] Design tokens render correctly (verified visually)
</verification>

<success_criteria>
- All tasks completed
- Tailwind v4 configured with all Zunoora design tokens as utility classes
- Custom CSS utilities available app-wide
- App shell renders carbon dark theme
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-02-SUMMARY.md`
</output>
